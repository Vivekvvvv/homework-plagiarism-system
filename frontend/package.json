{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "npm run test:unit",
    "test:unit": "node -e \"require('fs').rmSync('.test-dist',{ recursive: true, force: true })\" && tsc -p tsconfig.test.json && node ./tests/run-tests.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.4",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vue-tsc": "^3.2.4"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "element-plus": "^2.13.2",
    "jspdf": "^2.5.2",
    "pinia": "^2.3.1",
    "vue": "^3.5.28",
    "vue-router": "^4.5.1"
  }
}
